export const BATTLE_NUMBERS = {
  tickRate: 30,
  tickMs: 33.33,
  snapshotRate: 15,
  snapshotMs: 66.67,
  inputRate: 20,
  inputMs: 50,
  controlTimeLimitSeconds: 180,
  startLevel: 1,
  maxLevel: 10,
  levelUpEverySeconds: 15,
  baseCharacterRadius: 34,
  projectileRadius: 12,
  projectileBaseSpeed: 8.4,
  rangedAutoFireIntervalMs: 1050,
  meleeContactDamageIntervalMs: 900,
  blinkDistance: 180,
  skill1CooldownMs: 8000,
  blinkCooldownMs: 9000,
  ultimateCooldownMs: 20000,
  ultimateAutoFireEverySeconds: 20,
  skillGaugeMax: 100,
};
